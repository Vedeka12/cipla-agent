{
  "name": "my5-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev"
  }
}
